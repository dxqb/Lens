[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lens"
version = "0.1.0"
description = "Microsoft Lens text-to-image model"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["lens"]
